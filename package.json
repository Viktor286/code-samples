{
  "name": "codesamples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc -w --listFiles --traceResolution",
    "lint": "concurrently 'npm run lint:eng' 'npm run lint:data' 'npm run lint:spec'",
    "lint:main": "concurrently 'npm run lint:eng' 'npm run lint:data'",
    "lint:eng": "eslint './Engineering/**/*.ts --fix --ignore-pattern '*.spec.ts'",
    "lint:data": "eslint './Data/**/**.ts' --fix --ignore-pattern '*.spec.ts'",
    "lint:spec": "eslint '**/**.spec.ts' -c '.eslintrc.spec.js' --fix",
    "format": "prettier \"**/*.ts\" --ignore-path ./.prettierignore --write && git status"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.20.0",
    "@typescript-eslint/parser": "^2.20.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.0",
    "prettier": "^1.19.1",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.2"
  },
  "extension": [
    ".ts"
  ]
}
